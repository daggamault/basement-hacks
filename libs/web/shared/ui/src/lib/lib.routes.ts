import { Route } from '@angular/router';
import { WebSharedUiComponent } from './web-shared-ui/web-shared-ui.component';

export const webSharedUiRoutes: Route[] = [
  { path: '', component: WebSharedUiComponent }
];
